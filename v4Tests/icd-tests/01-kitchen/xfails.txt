# Expected failures due to documented bugs (q/details-docs/icd-analysis.md par.2).
# Format: <check-name> <roadmap-id> [reason]
# Remove a line once the corresponding fix lands, and update the golden.
nav-labels R7 in-context tables reuse the bare TAS name (Samples/Position/Payload duplicated; roadmap B4)
html-anchors R6 colorizer emits no ASN1_<hash> anchor when a TAS name maps to multiple hashes (roadmap B5)
